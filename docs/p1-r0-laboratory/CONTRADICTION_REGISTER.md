# P1-R0 C0 contradiction register

This register distinguishes the attached pack and draft architecture from the controlling owner roadmap and the repository's actual frozen contracts. Resolutions must preserve the stronger boundary and must not silently reinterpret a biological fact.

| ID | Observation | Resolution | Blocking effect |
|---|---|---|---|
| C0-001 | The pack handoff is written for a pre-R110F PASS gate; production is now frozen at R123F and its benchmark is running. | Follow the owner's newer explicit schedule: isolated laboratory work may run during the R123F benchmark. Retain all pack prohibitions and stop before production attachment. | Does not block laboratory Tranche A/B; blocks production attachment. |
| C0-002 | The draft architecture presents HOMEOS before METAB, while its data dependencies and the approved roadmap require committed METAB summaries before HOMEOS can run. | Design and freeze the HOMEOS contract early so METAB exposes the correct canonical summaries. Implement and activate runtime cores in dependency order: METAB, HOMEOS, INTERO. | Blocks HOMEOS runtime until METAB summary fixtures pass. |
| C0-003 | The pack calls its frame object “UBE v2,” but the repository already has a frozen, incompatible `stay-biological-envelope-v2` protocol. | Do not replace, version-alias, or overload the repository protocol. Treat the pack object as a P1 causal-frame payload/adapter contract that is carried by a normal repository Envelope v2 proposal. | Blocks copying the pack schema under the existing UBE protocol name. |
| C0-004 | Pack signal classes (`SUMMARY`, `INTEGRITY`, `DEMAND`, `MODULATION`, `FACT`, `CONTEXT`) are not repository `SIGNAL_CLASS` wire values. | Add an explicit closed mapping reviewed per topic: summary/integrity to an allowed summary/evidence class, demand to afferent/evidence as appropriate, modulation to regulatory efferent, transition fact to biological transition, and Chronobiology context to chronobiological context. Unknown mappings fail closed. | Blocks route manifests until every topic has one exact mapping. |
| C0-005 | The pack models frame numbers and visibility frames; the repository's durable causality is expressed by accepted trusted microseconds, Fabric sequence, parents, source spans, stream progress, and route barriers. | Define a bounded P1 frame index derived only from committed trusted organism time and store the corresponding accepted-envelope evidence. `visibleFromFrame >= committedFrame + 1` is an additional consumer rule, not a replacement for repository causality. | Blocks same-frame consumption and wall-clock-derived frames. |
| C0-006 | The pack checkpoint schema duplicates information already committed atomically by resident checkpoints, consumers, and biological outbox intents. | Store the P1 core state inside the existing resident checkpoint transaction. Validate a P1 checkpoint projection against StateStore metadata instead of creating a second transition authority. | Blocks parallel cursor/checkpoint/output commit paths. |
| C0-007 | The pack requires founder and persistent chip history records; current StateStore has neither canonical record type. | Add laboratory-only, additive StateStore tables and APIs with immutable history, exact hashes, parent/generation fences, and P0 no-op migration tests. Do not deploy this migration during R123F. | Blocks foundation PASS until migration and rollback refusal tests exist. |
| C0-008 | Existing SNTSS fixed point is scale 1,000,000 and Chronobiology has a distinct frozen engine; the pack requires signed Q16.48 with half-even rounding. | Add an isolated P1 Q16.48 BigInt implementation and golden vectors. Do not import it into or alter SNTSS/Chronobiology numerical engines. | Blocks METAB math until all Q16.48 vectors pass. |
| C0-009 | The pack route registry says routes become active in an owner-authorized tranche, while the roadmap requires routes absent through LAB/NEUTRAL and no production attachment before benchmark PASS plus separate authorization. | Compile and validate route declarations in the laboratory, but keep runtime route registration absent until each core is accepted and the later attachment gate is explicitly authorized. The SNTSS receptor route is last and revocable. | Blocks all live route registration. |
| C0-010 | The pack chip order contains future P2+ systems, while the current roadmap fixes the public R0 order to six entries. | P1-R0 public rail order is exactly BSF, SNTSS, CHRONOBIOLOGY, METAB, HOMEOS, INTERO. Future systems stay absent; they are not chips or roadmap labels in this tranche. | Blocks AUTON/CARD/RESP and later UI state. |
| C0-011 | The direct-Windows full suite exercises the repository but cannot satisfy Linux symlink, Unix-socket, POSIX-signal, Bubblewrap, and host-root tests. | Preserve the Windows result as diagnostic only. Tranche PASS requires the unmodified full suite and real entry preflights on Linux. Do not skip, rewrite, or relax those tests to obtain a pass. | Blocks tranche PASS until Linux CI/clean extraction passes. |
| C0-012 | The pack requests a 24-hour P1 qualification, while the production hardening roadmap currently records R123F milestones at 15 minutes, 12 hours, and 72 hours. | Do not reuse R123F as P1 evidence. R123F continues at 15m/12h/72h. Later P1 qualification retains its own fresh 15m/24h/72h campaign after attachment authorization. | Blocks treating the current benchmark as P1 qualification. |
| C0-013 | The pack's canonical `CoreChipRecordV1` requires `historyHeadHash`, but a caller cannot safely supply the head that StateStore must compute for the append-only chain; hashing the complete record into its own head would also be circular. | Accept a bounded internal `CoreChipObservationV1` operation with the same public fields except `historyHeadHash`. StateStore computes the next chain head from the observation hash and previous head, then atomically persists and returns the pack-canonical `CoreChipRecordV1`. The internal operation is never stored or projected as the persistent chip record. | Blocks caller-authored history heads and noncanonical persisted chip records. |
| C0-014 | A pack causal ancestor has no repository authority epoch, accepted signal ID, or range digest, so it cannot establish durable repository causality by itself. | Treat the P1 causal span as a validated payload claim only. Any claimed ancestry must be accompanied by Kernel-resolved Envelope-v2 parents or source spans, and accepted Envelope metadata remains authoritative for causality and anti-laundering. Consumers may not promote authority from the payload claim. | Blocks ancestry-free adaptation of a frame that claims neutral/shadow/transitive ancestry. |

## Fixed implementation order

1. Seal repository reality and the mappings above.
2. Add schema validation, P1 Q16.48, deterministic noise, causal-frame rules, founder/checkpoint projections, and red tests.
3. Add persistent founder/chip history to a cloned laboratory StateStore and prove P0 no-op migration.
4. Implement METAB absent, then neutral, then shadow-qualified in the laboratory.
5. Implement HOMEOS from committed future-frame METAB summaries only.
6. Implement INTERO from delayed committed METAB/HOMEOS summaries only.
7. Add one bounded, absent-by-default SNTSS interoceptive receptor route.
8. Rehearse recovery/removal/revocation on a cloned StateStore.
9. Stop before production attachment.
