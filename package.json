{
  "name": "genesis-stay-living-runtime",
  "version": "0.7.0",
  "private": true,
  "description": "STAY / Project Genesis 0.7 Living Runtime Foundation: persistent identity, event fabric and hot-swappable versioned cores.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "verify:release": "node scripts/verify-release.js"
  },
  "engines": {
    "node": ">=22"
  }
}
