'use strict';

/*
 * Generated by tools/generate-c3r4-general-kernel.js. C3RC.4 admits exactly
 * 64 founder long-range edges before this unrolled kernel is callable. The
 * arithmetic and operation order are the frozen C3RC.3 contract.
 */
const Q30_SPLIT = 32_768;
const Q20_RECIPROCAL = 9.5367431640625e-7;
const Q30_RECIPROCAL = 9.313225746154785e-10;
const ADAPTIVE_ROUNDING_GUARD = 0.00001;

function accumulateGeneralEdges(phases, sums, generalUnits,
  generalWeightHigh, generalWeightLow, generalWeightScale,
  sinValues, sinDeltas) {
  let units;
  let left;
  let right;
  let phase;
  let index;
  let fraction;
  let current;
  let scaled;
  let sine;
  let response;
  let sineMagnitude;
  let scaledProduct;
  let productFloor;
  let productFraction;
  let magnitude;
  let sineHigh;
  let sineLow;
  let weightHigh;
  let weightLow;
  let exactHigh;
  let exactLow;

  units = generalUnits[0];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[0];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[0];
      weightLow = generalWeightLow[0];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[1];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[1];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[1];
      weightLow = generalWeightLow[1];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[2];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[2];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[2];
      weightLow = generalWeightLow[2];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[3];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[3];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[3];
      weightLow = generalWeightLow[3];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[4];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[4];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[4];
      weightLow = generalWeightLow[4];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[5];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[5];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[5];
      weightLow = generalWeightLow[5];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[6];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[6];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[6];
      weightLow = generalWeightLow[6];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[7];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[7];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[7];
      weightLow = generalWeightLow[7];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[8];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[8];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[8];
      weightLow = generalWeightLow[8];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[9];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[9];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[9];
      weightLow = generalWeightLow[9];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[10];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[10];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[10];
      weightLow = generalWeightLow[10];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[11];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[11];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[11];
      weightLow = generalWeightLow[11];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[12];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[12];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[12];
      weightLow = generalWeightLow[12];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[13];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[13];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[13];
      weightLow = generalWeightLow[13];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[14];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[14];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[14];
      weightLow = generalWeightLow[14];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[15];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[15];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[15];
      weightLow = generalWeightLow[15];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[16];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[16];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[16];
      weightLow = generalWeightLow[16];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[17];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[17];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[17];
      weightLow = generalWeightLow[17];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[18];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[18];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[18];
      weightLow = generalWeightLow[18];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[19];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[19];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[19];
      weightLow = generalWeightLow[19];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[20];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[20];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[20];
      weightLow = generalWeightLow[20];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[21];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[21];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[21];
      weightLow = generalWeightLow[21];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[22];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[22];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[22];
      weightLow = generalWeightLow[22];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[23];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[23];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[23];
      weightLow = generalWeightLow[23];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[24];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[24];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[24];
      weightLow = generalWeightLow[24];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[25];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[25];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[25];
      weightLow = generalWeightLow[25];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[26];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[26];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[26];
      weightLow = generalWeightLow[26];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[27];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[27];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[27];
      weightLow = generalWeightLow[27];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[28];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[28];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[28];
      weightLow = generalWeightLow[28];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[29];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[29];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[29];
      weightLow = generalWeightLow[29];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[30];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[30];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[30];
      weightLow = generalWeightLow[30];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[31];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[31];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[31];
      weightLow = generalWeightLow[31];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[32];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[32];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[32];
      weightLow = generalWeightLow[32];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[33];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[33];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[33];
      weightLow = generalWeightLow[33];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[34];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[34];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[34];
      weightLow = generalWeightLow[34];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[35];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[35];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[35];
      weightLow = generalWeightLow[35];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[36];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[36];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[36];
      weightLow = generalWeightLow[36];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[37];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[37];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[37];
      weightLow = generalWeightLow[37];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[38];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[38];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[38];
      weightLow = generalWeightLow[38];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[39];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[39];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[39];
      weightLow = generalWeightLow[39];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[40];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[40];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[40];
      weightLow = generalWeightLow[40];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[41];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[41];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[41];
      weightLow = generalWeightLow[41];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[42];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[42];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[42];
      weightLow = generalWeightLow[42];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[43];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[43];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[43];
      weightLow = generalWeightLow[43];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[44];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[44];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[44];
      weightLow = generalWeightLow[44];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[45];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[45];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[45];
      weightLow = generalWeightLow[45];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[46];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[46];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[46];
      weightLow = generalWeightLow[46];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[47];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[47];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[47];
      weightLow = generalWeightLow[47];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[48];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[48];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[48];
      weightLow = generalWeightLow[48];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[49];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[49];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[49];
      weightLow = generalWeightLow[49];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[50];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[50];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[50];
      weightLow = generalWeightLow[50];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[51];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[51];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[51];
      weightLow = generalWeightLow[51];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[52];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[52];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[52];
      weightLow = generalWeightLow[52];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[53];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[53];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[53];
      weightLow = generalWeightLow[53];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[54];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[54];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[54];
      weightLow = generalWeightLow[54];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[55];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[55];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[55];
      weightLow = generalWeightLow[55];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[56];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[56];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[56];
      weightLow = generalWeightLow[56];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[57];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[57];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[57];
      weightLow = generalWeightLow[57];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[58];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[58];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[58];
      weightLow = generalWeightLow[58];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[59];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[59];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[59];
      weightLow = generalWeightLow[59];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[60];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[60];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[60];
      weightLow = generalWeightLow[60];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[61];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[61];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[61];
      weightLow = generalWeightLow[61];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[62];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[62];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[62];
      weightLow = generalWeightLow[62];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

  units = generalUnits[63];
  left = units & 63;
  right = units >>> 6;
  phase = (phases[right] - phases[left]) >>> 0;
  index = phase >>> 20;
  fraction = phase & 0xf_ffff;
  current = sinValues[index];
  scaled = sinDeltas[index] * fraction;
  sine = current + (scaled < 0
    ? -(((-scaled + 524_288) * Q20_RECIPROCAL) | 0)
    : (((scaled + 524_288) * Q20_RECIPROCAL) | 0));
  response = 0;
  if (sine !== 0) {
    sineMagnitude = sine < 0 ? -sine : sine;
    scaledProduct = sineMagnitude * generalWeightScale[63];
    productFloor = scaledProduct | 0;
    productFraction = scaledProduct - productFloor;
    if (productFraction < 0.5 - ADAPTIVE_ROUNDING_GUARD
      || productFraction > 0.5 + ADAPTIVE_ROUNDING_GUARD) {
      magnitude = productFloor + (productFraction >= 0.5 ? 1 : 0);
    } else {
      sineHigh = sineMagnitude >>> 15;
      sineLow = sineMagnitude & 0x7fff;
      weightHigh = generalWeightHigh[63];
      weightLow = generalWeightLow[63];
      exactHigh = sineHigh * weightHigh;
      exactLow = (sineHigh * weightLow + sineLow * weightHigh) * Q30_SPLIT
        + sineLow * weightLow;
      magnitude = exactHigh
        + Math.floor((exactLow + 536_870_912) * Q30_RECIPROCAL);
    }
    response = sine < 0 ? -magnitude : magnitude;
  }
  sums[left] += response;
  sums[right] -= response;

}

module.exports = { accumulateGeneralEdges };
